import { Command } from 'commander';
import { ConfigManager } from '../config';
/**
 * Register configuration and setup commands with commander
 */
export declare function registerConfigCommands(program: Command): void;
/**
 * Initialize configuration on CLI startup
 */
export declare function initializeConfig(): ConfigManager;
//# sourceMappingURL=config-commands.d.ts.map